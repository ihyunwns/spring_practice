spring:
  # spring 설정이 없으면 스프링부트는 자동으로 메모리 모드로 실행시켜 준다.
  # 혹은 저렇게 메모리 모드를 지원하는 데이터베이스 같은 경우는 메모리 모드 url을 적어준다.
#  datasource:
    #h2 database 메모리 모드
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver

#  jpa:
#    hibernate:
#      ddl-auto: create
#
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true



logging:
  level:
    org.hibernate.sql: debug
    org.hibernate.type: trace

